# Merge two GWAS VCF objects

Returns merged intersection of two VCF objects

## Usage

``` r
merge_vcf(a, b)
```

## Arguments

- a:

  VCF object

- b:

  VCF object

## Value

SimpleList of VCF objects
