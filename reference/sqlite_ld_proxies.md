# Lookup LD proxies from sqlite database

Lookup LD proxies from sqlite database

## Usage

``` r
sqlite_ld_proxies(rsids, dbfile, tag_r2)
```

## Arguments

- rsids:

  List of rsids

- dbfile:

  path to dbfile

- tag_r2:

  minimum r2 value

## Value

data frame
